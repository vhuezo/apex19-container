source /home/oracle/.bashrc

${ORACLE_HOME_BUILD}/bin/dbstart ${ORACLE_HOME_BUILD}

${ORACLE_HOME_BUILD}/bin/sqlplus / as sysdba @/tmp/inst_apex.sql

${ORACLE_HOME_BUILD}/bin/sqlplus / as sysdba<<EOF
alter session set container=pdb1;
@apex_rest_config.sql ApexOrdsPass1 ApexOrdsPass1
exit;
EOF
